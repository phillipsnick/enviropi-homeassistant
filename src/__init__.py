"""src package: renamed from original 'enviropi'.

This package contains the service, sensors, MQTT utilities, and display handling
for Enviro+ to Home Assistant via MQTT.

The runtime behavior and MQTT topics remain unchanged from the original
package; only the package directory name has changed to `src` to match project
structure preferences.
"""
